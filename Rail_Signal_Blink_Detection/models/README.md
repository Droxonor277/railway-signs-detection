# Model weights

Place the YOLO model weights in this folder, for example:

```text
yolo11s_n6.pt
```

The `.pt` file is not included in this Git package because model weights are large binary files and should normally be stored outside Git or via Git LFS.
